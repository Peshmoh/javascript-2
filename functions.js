//function declaration

function greet(){
    console.log('hello there');
}

/*greet();

//function expression

const speak = function(){
    console.log('good day?')
};

speak();


//arguments and parameters
var day = function(name = 'esy', time = 'night'){
    console.log(`good ${time} ${name}`);
};


day('peris', 'morning');
 

const calcarea = radius =>{
    return 3.14 * radius **2;

};

const areaz = calcarea(5);
console.log('area is:', areaz);

//practice arrow functions

const greeti = function(){
    return 'hello,world';
}

const greeting = () =>'hello world';


const result =  greeting();
console.log(result)


const bill = function(product, tax){
    let total = 0;
    for(let i = 0;i < product, length;i++){
        total += product[i] + product[i] * tax;
    }
    return total;
}

console.log(bill([10,15,30],0.2));


  const bills =(product ,tax) => {
    let total = 0;
    for(let i = 0; i <product ,length;i++){
        total += products[i] + product[i] * tax;
    }
    return total;
  };
  
  console.log(bills([10, 15, 30],0.2));

  const name = 'shaun'
  //functions 

  const grt = () => 'hello'
  let resultOne = grt();

  console.log(resultOne);

  //methods
  let resultTwo = name .toUpperCase();
  console.log(resultTwo);

 */